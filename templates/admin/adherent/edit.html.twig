{% extends 'base.html.twig' %}

{% block title %}Modification de la fiche appartenant à
	{{adherent.lastName}}
	{{adherent.firstName}}
{% endblock %}

{% block body %}

	<div class="container">
		<h1 class="my-5">Modification de la fiche adhérent de
			{{adherent.lastName}}
			{{adherent.firstName}}</h1>


		{{ form_start(form) }}

		{{ form_widget(form) }}

		<button class="btn btn-primary" type="submit">
			<i class="fas fa-check"></i>
			Enregistrer les modifications
		</button>

		{{ form_end(form) }}


	</div>

</div>{% endblock %}

