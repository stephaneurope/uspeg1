{% extends "base.html.twig" %}

{% block title %}Modification de mot de passe
{% endblock %}

{% block body %}
	{% set name = "Modification du mot de passe"  %}
	{% include "header_title.html.twig" %}
	<div class="container">


		{{ form_start(form) }}
		{{ form_widget(form )}}
		<button type="submit" class="btn btn-primary float-right">
			<i class="fas fa-check"></i>
			Modifier le mot de passe</button>
		{{ form_end(form) }}
	</div>

{% endblock %}
