
<!-- header #4 -->
<header class="header mb-4 " id="header4">
<nav class="navbar navbar-expand-lg navbar-dark bg-transparent" style="z-index:5";>
	<img style="width: 60px;height:60px;" src="/image/logo/uspeg.png" alt="logo uspeg">
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"><i class="fas fa-bars fa-3x" style="color:#fff; font-size:28px;"></i></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarColor01">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item active">
				<a class="nav-link" href="{{ path('home') }}">Home
					<span class="sr-only">(current)</span>
				</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('adherent') }}">Adhérents</a>
			</li>
			<li class="nav-item">
				<a class="nav-link" href="{{ path('category') }}">Boutique</a>
			</li>
			{% if is_granted('ROLE_ADMIN') %}
			<li class="nav-item">
				<a class="nav-link" href="{{ path('dashboard') }}">Tableau de Bord</a>
			</li>
			{% endif %}
		</ul>
		<ul class="navbar-nav ml-auto">

			
{% if app.user %}
			 <li class="nav-item dropdown" >
               <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
              {{app.user.lastName}} {{app.user.firstName}}
               </a>
               <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
               
               <div class="dropdown-divider"></div>
               <a href="{{path('account_index')}}" class="dropdown-item">Mon compte</a>
               
               <a href="{{path('account_profile')}}" class="dropdown-item">Modifier mon profil</a>
               <div class="dropdown-divider"></div>
               <a href="{{path('account_logout')}}" class="dropdown-item" >Déconnexion</a>
               </div>
           </li>
			{% endif %}
		</ul>
	</div>
</div></nav>
	<div class="center">
		<div class="caption">
			<h2 class="titre title display-3">{{name}}</h2>
			
		</div>	
	</div>

</header>
			{% for label, messages in app.flashes %}
<div class="container mt-2">
<div class="alert alert-{{ label }}">
{% for message in messages %}
  <p class="mt-2">{{ message | raw }}</p>
{% endfor %}
</div>
</div>
			{% endfor %}